# rag-pipeline
A production-structured RAG system built with Java 21 and Spring Boot that ingests documents and answers questions using a fully local LLM pipeline — no OpenAI, no cloud APIs, runs entirely on your machine via Ollama.
